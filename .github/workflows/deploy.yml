name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      ACCESS_TOKEN: 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

    steps:
      - name: Display custom enviroment variables
        run: |
          echo "The ACCESS_TOKEN is ${{ env.ACCESS_TOKEN }}"

      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - name: Install and Build 🔧
        run: |
          npm ci
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.0
        with:
          branch: gh-pages
          folder: dist
